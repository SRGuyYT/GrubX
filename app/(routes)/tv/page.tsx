import { CatalogGrid } from "@/components/media/CatalogGrid";

export default function TvPage() {
  return (
    <CatalogGrid
      mediaType="tv"
      title="TV Library"
      description="Browse TV with the same deduped infinite engine and explicit-click playback controls."
    />
  );
}
